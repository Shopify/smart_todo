containers:
  default:
    docker: circleci/ruby:2.5

steps:
- label: ":cop: Ruby style check"
  timeout: 5m
  dependencies:
  - bundler
  run: bin/rubocop
- label: ":ruby: Ruby tests"
  timeout: 5m
  dependencies:
  - bundler
  run: bundle exec rake test
